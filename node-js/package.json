{
  "name": "near-by-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js port of the near-by AWS Lambda",
  "main": "lambda_function.js",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.864.0",
    "@aws-sdk/lib-dynamodb": "^3.864.0"
  },
  "engines": {
    "node": ">=20"
  }
}
